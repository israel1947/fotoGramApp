import { ImageSanatizerPipe } from './image-sanatizer.pipe';

describe('ImageSanatizerPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageSanatizerPipe();
    expect(pipe).toBeTruthy();
  });
});
